# Assumptions (A1–A11)

A1 — finite hypothesis space
A2 — bounded scores: \( m > 0, s_v(h) \in [m,M] \)
A3 — connected communication graph
A4 — spectral gap \( \gamma > 0 \)
A5 — measurable updates
A6 — strict consistency gap
A7 — interior initialization
A8 — positivity preservation
A9 — bounded normalization stability
A10 — graph remains connected under single-node removal
A11 — uniform contractivity of operator \(T(s)\)
