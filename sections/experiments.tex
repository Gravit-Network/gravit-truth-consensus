\section{Experiments}

We verify:
1. KL contraction: \(\Phi(t+1) \le (1 - \gamma\eta) \Phi(t)\)
2. Fixed-point convergence
3. Byzantine phase transition at predicted threshold.

Experiments use Erdős–Rényi graphs (\(N=50\), \(k=5\)), 30 runs per \(\beta\).
